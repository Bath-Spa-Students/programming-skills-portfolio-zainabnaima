# Exercise 3: T-Shirt

def make_shirt(size,message):
    print(f"Customizing a T-shirt with size '{size}' and message: '{message}'")

# Call the function using positional arguments
make_shirt("Small", "Dubai City")

# Call the function using keyword arguments
make_shirt(size="Large", message="Palestinians are beautiful")
